# Project Mandates (BOJ Analysis)

## 開発上の重要ルール
- **Jupyter Notebookの整合性 (最優先)**: 
  - `.ipynb` ファイルを作成・修正する際は、`write_file` ツールを直接使わず、**Pythonの `json` ライブラリを使用してプログラム経由で生成・保存すること**。
  - これにより、コントロールキャラクターのエラー（`\n` のエスケープ不備等）を物理的に排除する。
- **コードのモジュール化**: 
  - 前処理（`src/processing.py`）、特徴量生成（`src/features.py`）、モデル（`src/modeling.py`）のロジックは必ずスクリプトに抽出し、ノートブックからはこれらを呼び出す構成を維持する。
- **実験管理**: 
  - 試行錯誤（分数階差のパラメータ $d$ など）を効率化するため、`src/` 内の関数に引数として持たせる設計を徹底する。

## 分析の技術的選択
- **手法**: Vector-leaf GBDT (CatBoost MultiRegression)。
- **ターゲット**: 各会合の期待金利スプレッド（M1〜M8）の5日先予測。
- **アプローチ**: 
    - **BOJ Swap**: 現在の政策金利を引いた「スプレッド」を水準として使用。
    - **外部指標**: $d=0.4$ 程度の「分数階差」による定常性確保。
    - **補完**: MICEによる断面相関補完。補完箇所をモデルに伝えるためのバイナリフラグ（`M(n)_is_imputed`）を全会合で生成・保持。
